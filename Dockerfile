# Базовый образ
FROM node:14

# Установка рабочей директории
WORKDIR /app

# Копирование файлов в контейнер
COPY server.js .

# Экспонируем порт 80 для доступа к серверу
EXPOSE 80

# Команда запуска сервера
CMD ["node", "server.js"]